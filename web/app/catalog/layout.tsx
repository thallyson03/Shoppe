import type { ReactNode } from 'react';

export const metadata = {
  title: 'Shoppe · Catálogo',
  description: 'Ofertas Shopee com link de afiliado',
};

export default function CatalogLayout({ children }: { children: ReactNode }) {
  return children;
}
